<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.myapp.webtoon_downloader">

    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <service
            android:name=".Downlaodservice"
            android:exported="false"></service>
        <service
            android:name=".Downloadservice"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".notiRestartEvent"
            android:enabled="true"
            android:exported="true" /> <!-- receiver -->
        <receiver android:name=".updateCheck" />
        <receiver android:name=".notiCancelEvent" /> <!-- receiver -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Episode" />
        <activity android:name="com.myapp.webtoon_viewer.ViewerActivity" />
        <activity android:name="com.myapp.bookmark.Bookmark" />
        <activity android:name=".contextManager" />
        <activity android:name="com.myapp.webtoon_viewer.imageViewer" />

        <service android:name=".Download" />
    </application>

</manifest>